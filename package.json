{
  "name": "gitlab-mcp-server",
  "version": "1.0.0",
  "description": "Token-optimized self-hosted GitLab MCP server for Claude Code — multi-profile PAT management, issues, merge requests, pipelines",
  "module": "index.ts",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/DKeken/gitlab-mcp.git"
  },
  "keywords": [
    "gitlab",
    "mcp",
    "claude",
    "model-context-protocol",
    "claude-code",
    "self-hosted"
  ],
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.1.5"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
